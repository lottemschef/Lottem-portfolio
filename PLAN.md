# תוכנית בנייה — אתר תיק עבודות קולנועי · לוטם שיפנבאור

מסמך העבודה. נבנה לפיו שלב אחר שלב, עם אישור שלך בין שלב לשלב.

---

## 1 · החלטות שסוכמו

| נושא | ההחלטה |
|---|---|
| סטאק | HTML5 סמנטי + CSS3 טהור (Flexbox/Grid). בלי React, בלי Tailwind, בלי שלב build |
| שפה | דו-לשוני מלא — עברית RTL ואנגלית LTR, עם מתג החלפה. **ברירת המחדל: אנגלית** |
| הגרסה הישנה | הרוט מוחלף לגמרי באתר החדש |
| תמונות | מציני מקום בשלב הבנייה, החלפה לחומר אמיתי כשהתיקייה שלך מוכנה |

### לגבי ה-overwrite

הרוט של הפרויקט נמחק ונבנה מחדש — `index.html`, `src/`, `package.json`, `vite.config.js`,
`tailwind.config.js`, `postcss.config.js`. האתר החדש תופס את מקומם.

**לפני המחיקה אעתיק את הישן ל-`old/`.** זה לא סותר את ה-overwrite — הרוט יהיה נקי
ויכיל רק את האתר החדש. זו רק רשת ביטחון, למקרה שתרצה להציץ בטקסטים או בפרטי הקשר
שכתובים בקוד הישן. אם תגיד לי למחוק לגמרי — מחיקה בפקודה אחת. אני לא מוחק בלי שתאמר,
כי זו פעולה שאי אפשר לבטל.

---

## 2 · מה חולץ מהרפרנס

ה-PDF הוא צילום מסך אחד ברוחב 1872px וגובה 14400px. פוענח, נחתך ל-12 פרוסות,
והצבעים נדגמו ישירות מהפיקסלים — לא מהעין.

### הפלטה

| משתנה | ערך | תפקיד |
|---|---|---|
| `--accent-red` | `#D1250C` | כותרות ענק, תוויות קטגוריה, קווים מפרידים |
| `--bg-dark` | `#000000` | שחור מוחלט. לא `#111`, לא אפור כהה |
| `--bg-light` | `#F7F2E6` | קרם חמים — פרופיל, פורטרטים, יצירת קשר |
| `--bg-mute` | `#8A877F` | אפור-חול — רצועת נגני הווידאו בלבד |
| `--text-on-dark` | `#F7F2E6` | הקרם משמש גם כלבן-שבור על שחור |
| `--text-on-light` | `#0A0A0A` | |

### הזוהר האדום — הפרט שעושה את ההבדל

הדבר הכי קל לפספס בעיצוב הזה: **כל טקסט אדום זוהר.** יש לו הילת ניאון סביבו.

```css
text-shadow: 0 0 24px rgba(209, 37, 12, .55),
             0 0 72px rgba(209, 37, 12, .30);
```

בלי זה מקבלים טקסט אדום. איתו מקבלים ניאון. זה מקור התחושה הקולנועית של כל הדף.

### טיפוגרפיה

**עודכן אחרי בדיקת המקור ב-Behance.** הרפרנס האמיתי הוא מצגת של 13 שקופיות 16:9,
והרזולוציה הגבוהה חשפה שכל הכותרות — מ-PORTFOLIO ועד "Landscape Photography" —
הן **משפחה גיאומטרית-מעוגלת אחת**, לא שתיים.

| תפקיד | הפונט |
|---|---|
| כל הכותרות, שתי השפות | **Rubik** 800–900 |
| טקסט זורם, שתי השפות | **Assistant** 400–600 |

Rubik תומך בעברית ובלטינית באותו קובץ, כך שהגרסה העברית והאנגלית חולקות טיפוגרפיה —
מה שמייתר את הפיצול שתכננתי קודם ומקטין את המטען מ-212KB ל-80KB.

אם תרצה התאמה של 100%, המקור נראה כמו Gilroy או Sofia Pro. אפשר לרכוש ולארח —
החלפה של שורה אחת ב-CSS.

---

### מה שהמקור באיכות מלאה שינה

הצילום המכווץ הסתיר פרטים שהמקור חשף. אלה התיקונים שנכנסו:

- **הפורטרטים חוצים את הגבול.** בשקופית הפרופיל ובשקופית יצירת הקשר הרקע מתחלק
  לשניים — קרם למעלה, שחור למטה — והפורטרט מונח על התפר ונמשך דרכו.
  זה מה שמונע מהעמוד להיראות כמו רצף של רצועות.
- **השם האדום יושב בתוך מסגרת התמונה**, לא מחוצה לה. רק הזוהר גולש.
- **הביו מיושר לשני הצדדים** (justify).
- **הזוהר חזק יותר** משהערכתי: שלוש שכבות — ליבה בהירה, פריחה, והילה רחבה.
- **טבלת יצירת הקשר תופסת חצי עמוד בלבד**, והערכים דחוקים לקצה הרחוק מול התוויות.
  שורת האינסטגרם מסומנת בקו תחתון גם במנוחה — היא היחידה שמובילה לאנשהו.
- **המרווחים דקים יותר**: שוליים של 5.7% מרוחב העמוד, ומרווח בין אריחי המוזאיקה
  של כ-0.6% בלבד.
- **הטיפול לא אחיד בין המקטעים**: בצילום נוף זוג התמונות המובילות ממוסגר בשוליים,
  ובצילום אירועים הוא נמתח מקצה לקצה. אשמור על ההבדל.

## 3 · מפת הדף

14 מקטעים. עמודת הרקע היא המקצב הוויזואלי של הדף — שחור, קרם, שחור, אפור, קרם.

| # | מקטע | רקע | מה יש בו |
|---|---|---|---|
| 1 | כותרת פתיחה | שחור | שם למעלה-שמאל, שנה למעלה-ימין, **PORTFOLIO** אדום ענק |
| 2 | תמונת פתיחה | — | נוף אולטרה-רחב, לרוחב מלא |
| 3 | My Profile | קרם | פורטרט משמאל, ביו מימין |
| 4 | רזומה | שחור | הפורטרט ממשיך לתוך השחור עם השם באדום זוהר עליו; מימין Education / Skills / Expertise עם קווים אדומים |
| 5 | כותרת עבודות | שחור | **My Works.** לבן משמאל, **PHOTOGRAPHY** אדום זוהר מימין |
| 6 | תמונת נושא | — | לרוחב מלא |
| 7 | Landscape | שחור | כותרת אדומה, שתי תמונות מובילות, שורות Location/Year, פסקת סיפור |
| 8 | מוזאיקת נוף | שחור | שלוש שורות של 4 תמונות ברוחבים לא שווים, מקצה לקצה |
| 9 | Event | שחור | אותו דפוס: 2 מובילות + מטא + פסקה, ואז מוזאיקה עם פורטרט גבוה שתופס 2 שורות |
| 10 | Portrait | קרם | כותרת אדומה משמאל, פורטרט גדול + בינוני עם פסקה |
| 11 | שורת פורטרטים | קרם | שלושה פורטרטים |
| 12 | VIDEOGRAPHY | שחור | כותרת ענק על סטיל מהסרט, **My Works.** מימין למטה |
| 13 | Color Grading | שחור | זוג תמונות + מטא + פסקה, ואז גריד וידאו בפינות מעוגלות |
| 14 | נגני וידאו | אפור-חול | שלושה נגנים, כפתור Play לבן, תוויות אדומות מתחת |
| 15 | יצירת קשר | קרם | טבלה עם קווים + **WORK WITH ME!** ענק + פורטרט סיום |

---

## 4 · מבנה הקבצים

```
lotem-portfolio-aws/
├── index.html              # הדף כולו — HTML סמנטי אחד
├── css/
│   ├── base.css            # reset, משתנים, פונטים, טיפוגרפיה
│   ├── layout.css          # מקטעים, גרידים, מוזאיקה
│   └── components.css      # כרטיסי תמונה, נגנים, טבלת קשר, ריחופים
├── js/
│   ├── i18n.js             # מילון HE/EN + מתג השפה
│   └── ui.js               # משווה לפני/אחרי, נגנים, גילוי בגלילה
├── assets/
│   ├── fonts/              # woff2 מקומיים (שלב הליטוש)
│   ├── photos/             # התמונות שלך
│   └── video/              # סרטונים + תמונות תצוגה
├── PLAN.md                 # המסמך הזה
├── PHOTOS.md               # מפת המשבצות להחלפת תמונות
├── README.md               # הוראות פריסה ל-AWS
└── old/                    # גיבוי פרויקט React הקודם
```

### למה מתג שפה ב-JS ולא שני קבצי HTML

שני קבצים היו מחייבים לבצע כל שינוי עיצובי פעמיים — מקור ודאי לסטיות לאורך זמן.
במקום זה: מקור אמת אחד, עם `data-i18n` על כל אלמנט טקסט ומילון ב-`js/i18n.js`.
המתג מחליף `lang`, `dir` והטקסטים, וזוכר את הבחירה ב-localStorage.
זיהוי אוטומטי לפי שפת הדפדפן בכניסה ראשונה, ופרמטר `?lang=en` לקישורים לשליחה.

**העלות:** מנועי חיפוש יאנדקסו בעיקר את שפת ברירת המחדל. אם קידום אורגני בשתי השפות
חשוב לך, נוסיף בשלב הליטוש גם `en/index.html` שנוצר מאותו מקור. תגיד לי אם זה על הפרק.

### RTL בלי לשבור את העיצוב

הנקודה העדינה בגרסה העברית:

- **טקסט, פרופיל, טבלת קשר, שורות Location/Year** — מתהפכים. נשתמש בתכונות לוגיות
  (`margin-inline-start`, `padding-inline`, `border-inline-start`) כך שההיפוך קורה מעצמו.
- **גרידי התמונות** — **לא** מתהפכים. סידור המוזאיקה הוא החלטה קומפוזיציונית, לא כיוון קריאה.
  נאלץ `direction: ltr` על מיכלי הגריד כדי לשמר אותו בדיוק.

---

## 5 · מציני מקום, ואיך תיראה התיקייה שתכין

### איך זה עובד בזמן הבנייה

לכל `<img>` באתר יש שם משבצת: `data-slot="landscape-tile-04"`.
כל משבצת נעולה ליחס גובה-רוחב ב-CSS דרך `aspect-ratio`, כך שהפריסה **לא תזוז**
כשהתמונות האמיתיות ייכנסו. מה שתראה עם מציני המקום הוא בדיוק מה שתקבל.

בזמן הבנייה: תמונות Unsplash בקישור ישיר. ההחלפה בסוף = הפלת קבצים לתיקייה
והחלפת בסיס הנתיב במקום אחד.

> תמונות Unsplash הן פיגום זמני. הן לא יכולות להישאר באתר חי של צלם — גם מסיבות
> רישוי וגם כי כל תמונה נטענת משרת זר ומאטה את האתר.

### מה לשים בתיקייה — 47 תמונות + 3 סרטונים

לפי הספירה מהרפרנס. **המספרים האלה גמישים** — תגיד לי כמה תמונות יש לך בפועל בכל
קטגוריה ואתאים את הגרידים. עדיף גריד קצר ומלא מגריד ארוך וחצי ריק.

| שם המשבצת | כמות | יחס | רזולוציה מומלצת |
|---|---|---|---|
| `hero-wide` | 1 | 21:9 | 3000 × 1286 |
| `profile-portrait` | 1 | 2:3 | 1200 × 1800 |
| `photo-hero` | 1 | 16:9 | 2400 × 1350 |
| `landscape-featured-01..02` | 2 | 16:9 | 2000 × 1125 |
| `landscape-tile-01..12` | 12 | 3:2 | 1400 × 933 |
| `event-featured-01..02` | 2 | 16:9 | 2000 × 1125 |
| `event-tile-01..07` | 7 | 3:2 (אחת 2:3 לגובה) | 1400 |
| `portrait-featured-01..02` | 2 | 3:4 | 1600 × 2133 |
| `portrait-tile-01..03` | 3 | 3:4 | 1200 × 1600 |
| `video-hero` | 1 | 21:9 | 3000 × 1286 |
| `grade-featured-01..02` | 2 | 16:9 | 2000 × 1125 |
| `video-tile-01..09` | 9 | 16:9 | 1400 × 788 |
| `video-poster-01..03` | 3 | 16:9 | 1920 × 1080 |
| `footer-portrait` | 1 | 3:4 | 1200 × 1600 |

**פורמט:** JPG באיכות 82–85, sRGB. אל תדחס מראש — אני אייצר גרסאות WebP ואת
כל גדלי ה-`srcset` בשלב הליטוש. תן לי את האיכות הגבוהה.

**שמות:** בדיוק כמו בעמודה הראשונה. אם תשמור ככה, ההחלפה היא העתקת תיקייה אחת
ואפס עבודת ידיים.

**סרטונים:** שלושה — Promo/Documentary, Travel, Social Media. MP4, H.264, 1080p.
שאלה פתוחה בהמשך (סעיף 9) לגבי איפה לארח אותם.

---

## 6 · השלבים

### שלב 0 — ארגון מחדש

העתקת הפרויקט הישן ל-`old/`, ניקוי הרוט, יצירת מבנה התיקיות החדש,
ועדכון ה-README. הפריסה ל-AWS מתפשטת משמעותית: נעלמים `npm install` ו-`npm run build`,
נשאר `aws s3 sync` בלבד.

### שלב 1 — יסודות

reset, `box-sizing: border-box`, `scroll-behavior: smooth`, כל משתני ה-CSS,
טעינת הפונטים, וסקאלה טיפוגרפית רספונסיבית מבוססת `clamp()` — קריטי, כי כותרת
שממלאת 1872px חייבת להתכווץ ל-375px בלי להישבר. בנוסף: שלד ה-HTML הסמנטי
עם כל ה-landmarks, ומנגנון ה-i18n.

**פלט:** דף שנטען, מקטעים ריקים אבל תחומים, מתג השפה עובד.

### שלב 2 — פתיחה ופרופיל

הכותרת הענקית עם הזוהר, השם והשנה, התמונה האולטרה-רחבה, מקטע הפרופיל בקרם,
ובלוק הרזומה השחור עם הפורטרט, השם האדום עליו, והעמודות עם הקווים האדומים.

### שלב 3 — גריד הצילום

החלק המורכב ביותר. גריד CSS אסימטרי עם `grid-column: span N` ורוחבי עמודות
לא שווים, שלוש תת-קטגוריות, בלוקי Location/Year עם יישור לעמודה קבועה,
פסקאות הסיפור, וזום עדין בריחוף בתוך `overflow: hidden`.

### שלב 4 — וידאו

כותרת VIDEOGRAPHY על תמונת רקע, משווה לפני/אחרי לקורקציית צבע
(CSS + כ-15 שורות JS, גרירה + מקלדת), גריד הווידאו המעוגל,
ורצועת הנגנים האפורה עם כפתורי Play שפועמים בריחוף.

### שלב 5 — יצירת קשר וליטוש

טבלת הקשר עם `mailto:` / `wa.me` / אינסטגרם ואנימציית קו תחתון,
כותרת WORK WITH ME!, פורטרט הסיום. ואז מעבר ליטוש מלא:

- **רספונסיביות** — נקודות עצירה ב-1440 / 1024 / 768 / 480. הגרידים האסימטריים
  מתקפלים לשתי עמודות ואז לאחת; הכותרות הענקיות מתכווצות ברציפות.
- **נגישות** — ניגודיות AA, מצב פוקוס נראה, `alt` אמיתי לכל תמונה,
  `prefers-reduced-motion`, ניווט מקלדת מלא.
- **SEO** — כותרת, תיאור, Open Graph, `hreflang`, JSON-LD מסוג `Person` +
  `ProfessionalService` (חשוב לחיפוש מקומי).
- **ביצועים** — `loading="lazy"` מתחת לקיפול, `srcset` לכל תמונה,
  WebP עם נפילה ל-JPG, אירוח הפונטים מקומית.
- **בדיקה** — הרצה חיה בדפדפן ובדיקה במובייל.

---

## 7 · פריסה ל-AWS אחרי המעבר

בלי שלב build, התהליך מתקצר:

```bash
aws s3 sync . s3://lotem-portfolio-site --delete \
  --exclude "old/*" --exclude "*.md" --exclude ".git/*"
aws cloudfront create-invalidation --distribution-id <ID> --paths "/*"
```

ה-README המעודכן יכיל את התהליך המלא: יצירת bucket, CloudFront עם OAC,
תעודת SSL ודומיין אישי.

---

## 8 · מה חסר לי

אפשר להתחיל בלי זה — אשים מציין מקום ברור לכל פריט — אבל בשלב 5 נצטרך את האמת:

| פריט | מצב |
|---|---|
| כתובת מייל עסקית | חסר (בקוד הישן `lotem@example.com`) |
| מספר וואטסאפ, פורמט בינלאומי | חסר (בקוד הישן `972501234567` — מציין מקום) |
| קישור לאינסטגרם העסקי | חסר |
| השכלה והכשרות למקטע Education | חסר |
| מספר רישיון הפעלת רחפן מסחרי | חסר — שווה מאוד להציג, זה מבדל אותך |
| שמות מקומות ושנים לשורות Location / Year | חסר |
| התמונות והסרטונים | בהכנה אצלך |

---

## 9 · שאלות פתוחות

1. **אירוח הסרטונים.** קובץ MP4 של דקה ב-1080p שוקל 15–25MB. שלושה כאלה = עד 75MB
   שנטענים מ-S3 בכל צפייה, וזה גם עולה כסף בתעבורה וגם מאט. החלופה: העלאה ל-YouTube
   או Vimeo כ"לא רשום" והטמעה. פחות שליטה בעיצוב הנגן, אבל מהיר וזול. **המלצה:**
   הטמעה, אלא אם חשוב לך נגן בלי שום מיתוג זר.

2. **דומיין.** יש לך דומיין קנוי, או שצריך לרכוש? זה משפיע על שלב ה-CloudFront.

3. **עמוד אנגלי נפרד ל-SEO** — צריך או לא? (ראה סעיף 4)

---

## 9א · ביקורת שלבים 1–4

בוצעה לפני המעבר לשלב 5. מה שנמצא ותוקן:

| מה | חומרה |
|---|---|
| `--f-sub` נשאר בשימוש אחרי החלפת הפונטים ולא היה מוגדר — שורת השם והשנה, כפתורי השפה, תוויות הנגנים ופס הבנייה נטענו ב-Assistant במקום ב-Rubik | **באג** |
| תמונת הפתיחה הייתה 21:9; מדידה חוזרת מה-PDF מראה **3.9:1** | פער מהמקור |
| באנר ה-VIDEOGRAPHY היה 21:9; במקור זו שקופית **16:9** מלאה | פער מהמקור |
| Skills ו-Expertise היו `<h4>` תחת כותרת רישוי — הן כותרות אחיות, לא תת-סעיף | סמנטיקה |
| כותרת "יצירת קשר" הנסתרת לא תורגמה | i18n |
| כללי CSS שהוחלפו ונשארו מתים | ניקיון |

נוסף גם **מסך טעינה למעבר בין השפות** — וילון שחור עם קו אדום זוהר.
ההחלפה מתרחשת מאחוריו, כי היא מהפכת כיוון כתיבה, מחליפה מטריקות פונט ומודדת
מחדש את כל הכותרות הענקיות בבת אחת. סה"כ 700ms, עם הגנה מלחיצות כפולות
ודילוג מלא כשהמשתמש ביקש פחות תנועה.

**נבדק ואומת:** אפס גלישה אופקית ב-375 / 654 / 1280 בשתי השפות, כל כותרת ענק
בתוך התיבה שלה, כל תמונה עם `alt` וממדים מפורשים, כל אלמנט אינטראקטיבי עם מעבר
בריחוף, היררכיית כותרות תקינה, וכל היחסים מול המקור:

| | המקור | אצלי |
|---|---|---|
| תמונת פתיחה | 3.9:1, מקצה לקצה | 3.9:1 ✓ |
| באנר וידאו | 16:9 | 16:9 ✓ |
| יחס עמודות הפורטרט | 1 : 2.1 : 1.41 | 1 : 2.10 : 1.41 ✓ |
| מרווח זוג הנוף | 4.4% | 4.4% ✓ |
| מוזאיקת נוף | 4/2/4/2 · 2/3/4/3 · 3/3/2/4 | זהה ✓ |
| הפורטרט חוצה את התפר | כן | כן ✓ |

## 9ב · סבב QA

בוצע אחרי שלב 5. מה שתוקן:

| ממצא | תוצאה |
|---|---|
| אין `srcset` — טלפון הוריד קבצי 1600px לתיבות 186px | 6 רוחבים × WebP/JPG. **12MB → 1.87MB, ירידה של 84%** |
| תמונת הפתיחה 96px במובייל | חיתוך 16:9 ייעודי ב-`<picture>` — 211px |
| הפורטרט נערם ראשון, מנותק מהתוכן | סדר מתוקן: טקסט → פורטרט → רצועה שחורה, בשני המקטעים |
| כפתורי שפה 38×28 | 44×44 |
| החלפת שפה שקטה לקוראי מסך | אזור `aria-live` שמכריז על השינוי |
| יישור דו-צדדי בעברית פותח נהרות | יישור באנגלית בלבד; עברית מיושרת לצד אחד |
| אין אנימציות גלילה | הופעה מדורגת ב-IntersectionObserver, 45 בלוקים |
| כפתור Play בלי הזמנה במגע | הילה פועמת, כבויה תחת `prefers-reduced-motion` |
| נגן שנכשל = ריבוע שחור | הודעת שגיאה בשתי השפות |
| תוויות אדומות על אפור, 1.47:1 | טקסט בדיו (5.5:1) + קו אדום זוהר תחתיו |
| `fitAllHeadings` בכל פריים של שינוי גודל | debounce של 120ms |
| `languagechange` מתנגש עם אירוע תקני | `lotem:languagechange` |
| לחיצה שנייה מהירה נזרקה | נשמרת לתור |
| פונטים עבריים בלי preload | נרמזים כשנכנסים דרך `?lang=he` |
| אין robots/sitemap | נוספו |
| ל-JSON-LD אין כתובת | `addressCountry: IL` |

## 10 · מצב סופי

כל חמשת השלבים בוצעו. מה שנשאר פתוח:

| פריט | מצב |
|---|---|
| 7 משבצות פורטרט | ממתינות לתמונות שלך — ראה PHOTOS.md |
| מיקומים ושנים לשורות Location / Year | ממתינות לפרטים ממך |
| `og:image` מוחלט + canonical | דורש דומיין |
| ניגודיות התוויות על הרצועה האפורה | טופל — ראה למטה |

### הפער היחיד שהשארתי בכוונה

תוויות הנגנים אדומות על רצועה אפורה — יחס ניגודיות **1.47:1**, הרבה מתחת לסף
של 3.0 לטקסט גדול. זה לא באג שלי: **זה מה שהמקור עושה.**

בדקתי אם אפשר לתקן בלי לשנות את המראה, והתשובה שלילית. אפור בינוני הוא הרקע
הגרוע ביותר לאדום — הבהירות שלהם קרובה מדי. להבהיר את הרצועה עד לעמידה בסף
היה הופך אותה לאפור בהיר כמעט לבן; להכהות אותה היה הופך אותה לשחורה. שום גוון
אדום קרוב ל-`#D1250C` לא עובר על הרקע הזה.

**שתי אפשרויות אמיתיות, שתיהן חריגה מהמקור:**

1. תוויות בשחור על האפור — 5.52:1, עובר בנוחות
2. תוויות בקרם על האפור — 3.07:1, עובר בקושי לטקסט גדול

**מה שנעשה בסוף:** הטקסט עבר לצבע הדיו (5.5:1) והאדום נשמר בקו זוהר מתחתיו.
זו החריגה הוויזואלית היחידה מהמקור באתר כולו. לשחזור מדויק:
`color: var(--accent-red)` ב-`.player__label`.

כל שאר הבדיקות עוברות: ניגודיות טקסט על שחור, על קרם, ועל האפור — הכול מעל AA.
